//Ext.desktopIconStore = function(){
  // track location of desktop icons, will be re-written to request data from server
  // and popuplate self, until then, load an emtpy store each page load, and add to
  // it as an icon is created
 // initComponent: function() {
//  {  var desktopIconStore = new Ext.data.SimpleStore({fields:['id','xPos','yPos'], data: []});}
 // }
//}
{}
